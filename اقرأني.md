# إيه اللي اتغير

المشكلة الأساسية كانت إن القراءة كانت شغالة بمكتبة اسمها Tesseract.js جوه المتصفح نفسه، وهي مكتبة ضعيفة جدًا في قراءة العربي، خصوصًا لما بتقرأ كل خلية لوحدها من غير أي سياق حواليها - عشان كده كانت بتلخبط الحروف بأرقام وتجيب رموز غريبة.

دلوقتي القراءة بقت بتتم عن طريق نموذج رؤية سحابي (Claude) بيقرأ الجدول كله (أو السطر كله) دفعة واحدة بسياق كامل، وده اللي بيدّي دقة أعلى بكتير في العربي المطبوع وخط اليد كمان.

## اللي اتغير في الملفات
- `index.html`: بدل ما كل خلية تتبعت لوحدها لـ Tesseract، بقى بيتقص الجدول كله (أو السطر كله في وضع الاستبدال بالصورة) وبيتبعت مرة واحدة لسيرفر بسيط. لو السيرفر مش متاح لأي سبب، فيه رجوع تلقائي (fallback) لنفس الطريقة القديمة عشان الأداة تفضل شغالة برضو.
- `api/scan.js`: سيرفر بسيط (Vercel Serverless Function) بياخد الصورة، يبعتها لـ Claude، ويرجع النص كـ JSON منظم بنفس عدد الصفوف والأعمدة اللي إنت محددهم.

## الخطوة الوحيدة المطلوبة منك
عشان القراءة السحابية تشتغل، محتاج تضيف مفتاح API واحد في إعدادات المشروع على Vercel:

1. اعمل حساب / سجل دخول على https://platform.claude.com وجيب API key من هناك.
2. في مشروعك على Vercel: Settings → Environment Variables.
3. ضيف متغير اسمه `ANTHROPIC_API_KEY` وقيمته المفتاح اللي جبته.
4. اعمل Redeploy للمشروع.

لو المفتاح مش متظبط، الأداة هترجع تلقائيًا للقراءة المحلية القديمة (أقل دقة) عشان محدش يتقفل برا الأداة، وهيظهر تحذير بسيط في الـ console.

## ملاحظة عن التكلفة
كل قراءة جدول أو صف = طلب واحد لنموذج Claude (مش طلب لكل خلية)، يعني التكلفة محدودة حتى لو الجدول كبير. لو عايز تتحكم في التكلفة أكتر أو حابب تجرب نموذج أرخص، غيّر قيمة `MODEL` في `api/scan.js`.
